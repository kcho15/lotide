const assertEqual = require('./assertEqual');

// Returns the first element of an array input
const head = function(input) {
  let result = input[0];
  return result; 
}

module.exports = head; 